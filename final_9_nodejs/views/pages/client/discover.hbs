<style>
/* CSS cho ảnh card */
.card-img-custom {
    width: 100%;
    height: 250px; 
    object-fit: cover;
}

/* KHU VỰC BANNER */
.hero-banner {
    height: 400px;
    /* Đảm bảo đường dẫn này chính xác so với thư mục public */
    background-image: url('/img/airlines/HCM/du-lich-sai-gon-ivivu.jpg'); 
    background-size: cover; 
    background-position: center center; 
    
    /* CĂN CHỈNH NỘI DUNG */
    display: flex;
    flex-direction: column;
    align-items: center; /* Căn giữa theo chiều ngang */
    justify-content: center; /* Căn giữa theo chiều dọc */
    color: white; /* Đảm bảo chữ trắng dễ đọc trên nền ảnh */
    text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.7);
    padding: 20px;
    text-align: center;
}

.hero-banner h2 {
    margin-bottom: 5px;
    font-size: 2.5rem;
}
.hero-banner p {
    font-size: 1.2rem;
}
/* Đảm bảo nội dung đủ chỗ để footer không đè */
.discover-page-container {
    padding-bottom: 200px;
}

    /* Footer không được absolute */
footer {
    position: relative !important;
    bottom: 0 !important;
}
</style>
<div class="discover-page-container">
<div class=" hero-banner">
    <h2>🏨 Khám Phá Các Điểm Nổi Bật tại {{pageTitle}}</h2> 
    <p>Dưới đây là danh sách các khách sạn và địa điểm du lịch hàng đầu.</p>
</div>

<div class="container mt-5">

    <div class="row row-cols-1 row-cols-md-2 g-4">
        
        {{#each hotels}}
        <div class="col">
            <div class="card shadow-sm h-100">
                <div class="row g-0">
                    <div class="col-md-5">
                        <img src="{{this.imageURL}}" class="img-fluid rounded-start card-img-custom" alt="{{this.name}}">
                    </div>
                    <div class="col-md-7">
                        <div class="card-body d-flex flex-column">
                            <h5 class="card-title">{{this.name}}</h5>
                            <p class="text-muted"><i class="fas fa-map-marker-alt"></i> {{this.address}}</p>
                            
                            <div class="d-flex justify-content-between align-items-center mb-2">
                                <div><span class="text-warning">
                                    {{#if (eq this.rating 5)}}★★★★★{{else}}★★★★☆{{/if}}
                                </span></div>
                                <div class="text-end">
                                    <span class="fw-bold fs-5 text-primary">{{this.score}} ({{this.reviews}})</span><br>
                                    <small class="text-success">Rất Tốt</small>
                                </div>
                            </div>
                            
                            <p class="card-text mb-3 small text-truncate">{{this.description}}</p>
                            
                            <div class="mt-auto d-flex justify-content-between align-items-center">
                                <div class="text-start">
                                    <small class="text-muted">Giá khởi điểm</small><br>
                                    <span class="fw-bold text-danger fs-5">USD {{this.price}}</span><small>/đêm</small>
                                </div>
                                <a href="/hotel/{{this._id}}" class="btn btn-primary btn-sm" style="background-color: #007bff; border-color: #007bff;">Xem Chi Tiết</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        {{/each}}
        
    </div>
</div>
</div>